{
	"name": "groundwork-common-volunteer-tracker",
	"private": true,
	"description": "Development tooling only. Nothing here is a runtime dependency, and nothing here reaches the release zip — see .distignore and hard rule 8 in CLAUDE.md.",
	"scripts": {
		"test:e2e": "playwright test",
		"test:e2e:ui": "playwright test --ui",
		"test:e2e:headed": "playwright test --headed",
		"test:e2e:report": "playwright show-report tests/e2e/.report",
		"test:js": "node tests/js/renumber.test.mjs"
	},
	"devDependencies": {
		"@playwright/test": "1.56.1"
	}
}
